# 태그노트

태그로 메모를 정리하는 PWA(Progressive Web App). Supabase 이메일·비밀번호 인증.

## 로컬 실행

1. `.env.example`을 복사해 `.env`로 저장하고 Supabase URL·anon 키를 넣습니다.
2. `npm install` 후 `npm run dev`
3. 프로덕션 빌드: `npm run build` → `dist`에 PWA 산출물이 생성됩니다.

## 스택

React, TypeScript, Vite, `vite-plugin-pwa`, Supabase Auth
