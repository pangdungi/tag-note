# Supabase 대시보드 → Settings → API
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# 개발(npm run dev)에서도 프로덕션처럼 구독 없으면 로그아웃하려면:
# VITE_ENFORCE_SUBSCRIPTION_IN_DEV=true

# 어디서든 구독 검사 끄기(임시 디버그만):
# VITE_SKIP_SUBSCRIPTION_GATE=true
